# BlockCity Empire MVP Starter

## Run backend
```bash
cd backend
npm install
npm run dev
```

## Run frontend
```bash
cd frontend
npm install
npm run dev
```

Set `VITE_SOCKET_URL` if backend not at `http://localhost:4000`.
